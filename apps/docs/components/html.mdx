---
title: "HTML"
sidebarTitle: "HTML"
description: "A React html component to wrap postcraft."
"og:image": "https://postcraft/static/covers/html.png"
icon: "file-code"
---

import Support from '/snippets/support.mdx'

## Install

Install component from your command line.

<CodeGroup>

```sh npm
npm install postcraft -E
```

```sh yarn
yarn add postcraft -E
```

```sh pnpm
pnpm add postcraft -E
```

</CodeGroup>

## Getting started

Add the component to your postcraft template. Include styles where needed.

```jsx
import { Html, Button } from "postcraft";

const Postcraft = () => {
  return (
    <Html lang="en" dir="ltr">
      <Button href="https://example.com" style={{ color: "#61dafb" }}>
        Click me
      </Button>
    </Html>
  );
};
```

## Props

<ResponseField name="lang" type="string" default="en">
  Identify the language of text content on the postcraft
</ResponseField>
<ResponseField name="dir" type="string" default="ltr">
  Identify the direction of text content on the postcraft
</ResponseField>

<Support/>
